const brandDAO = require('./Brand');
const orderDAO = require('./Order');
const productDAO = require('./Product');
const reviewDAO = require('./Review');
const noteDAO = require('./Note');
const userDAO = require('./User');

module.exports = {
  brandDAO,
  orderDAO,
  productDAO,
  reviewDAO,
  noteDAO,
  userDAO,
};
